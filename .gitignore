# Per-project state, written by /update-framework — not framework content
.github/.framework-version

# Per-project reports, generated by /report and /nextsteps — not framework content
PROGRESS_REPORT.md
NEXT_STEPS.md
FEATURE_TREE.md
FEATURE_TREE.json
# (FEATURE_TREE.example.json is committed framework content — not ignored)

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# Secrets — defense in depth; framework agents never read these anyway
.env
.env.*
!.env.example
